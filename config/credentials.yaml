# config\credentials.yaml
# v0.1.0

openai:
  api_key: "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

#response = openai.ChatCompletion.create(
    #model="gpt-3.5-turbo",
    #messages=[
    #    {"role": "system", "content": "Tu es un assistant de diagnostic système, explique comme à un humain simple."},
   #     {"role": "user", "content": "CPU usage: 91%, RAM: 82%, GPU: 77%. Pourquoi mon système lag ?"}
  #  ],
 #   max_tokens=200
#)

# Tu peux ajouter d’autres providers ou une section "tokens" si besoin.
